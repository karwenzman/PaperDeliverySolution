<Window x:Class="PaperDeliveryWpf.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PaperDeliveryWpf.Views"
        mc:Ignorable="d"
        Title="ShellView" Height="450" Width="800">
    <Grid>
        <!-- *** The main structure of the window is: Titel(Row=0), Header(Row=1), Body(Row=2) and Footer(Row=3) *** -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- *** The topmost frame of the UI *** -->
        <TextBlock Grid.Row="0" Style="{StaticResource textBlockHeader}">
            <Run FontWeight="Bold" FontStyle="Italic" FontSize="26" Foreground="Red">S</Run><Italic>udoku</Italic> 
            <Run FontSize="10" Text="ClickOnceVersion"/>
        </TextBlock>
        
        <local:ShellHeaderView Grid.Row="1" />
        <local:ShellBodyView Grid.Row="2" />
        <local:ShellFooterView Grid.Row="3" />
    </Grid>
</Window>
